# Vercel KV (Redis) connection details
# You'll need to get these values from your Vercel dashboard after setting up KV storage
KV_URL=redis://default:your_password@your_host:your_port
KV_REST_API_URL=https://your-project-url.vercel-storage.com
KV_REST_API_TOKEN=your_api_token_here
KV_REST_API_READ_ONLY_TOKEN=your_readonly_token_here

# Note: Replace the placeholders above with actual values from your Vercel dashboard
# Go to your Vercel project > Storage > KV Database > Connect (or Settings if already connected)
# The values will be displayed in the "Environment Variables" section 